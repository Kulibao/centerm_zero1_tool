# SATA LED settings managed by T-NAS Zero1tool.
# 1: blink green while a disk is in standby; 0: solid green while in standby.
STANDBY_BLINK=1
# 1: manage the corresponding disk LEDs; 0: keep both LEDs for that slot off.
LED1_ENABLED=1
LED2_ENABLED=1
